const { expect, assert, should } = require('chai');

describe('Admin', function() {
  // Sign in
  it('can sign in', function() {
    expect('1').to.equal('1');
  });

  it('can create trip', function() {
    expect('1').to.equal('1');
  });
});
